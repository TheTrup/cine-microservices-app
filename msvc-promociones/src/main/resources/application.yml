server:
  port: 8089 # Puerto exclusivo para promociones

spring:
  application:
    name: MSVC-PROMOCIONES
  datasource:
    url: jdbc:mysql://localhost:3306/cine_promociones_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
