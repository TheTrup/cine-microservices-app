server:
  port: 8085 # Puerto exclusivo para usuarios

spring:
  application:
    name: MSVC-USUARIOS
  datasource:
    url: jdbc:mysql://localhost:3306/cine_usuarios_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: # Pon tu contraseña si aplica
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/