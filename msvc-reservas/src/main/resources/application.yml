server:
  port: 8087 # Puerto exclusivo para reservas

spring:
  application:
    name: MSVC-RESERVAS
  datasource:
    url: jdbc:mysql://localhost:3306/cine_reservas_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: # Contraseña si aplica
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/